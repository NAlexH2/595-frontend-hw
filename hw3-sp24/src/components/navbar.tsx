export default function NavBar() {
    return (
        <nav>
            <a href={`/`}>Home</a>

            <a href={`/list`}>List</a>

            <a href={`/population`}>Population</a>

            <a href={`/customroute`}>Custom Route</a>
        </nav>
    );
}
